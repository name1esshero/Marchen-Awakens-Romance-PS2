# Work queue

Authority: [STANDARDS.md](STANDARDS.md). Updated: 2026-09-23.
Manager maintains this page and [status](STATUS.md) at each verified milestone.

## Delegation-ready reserve

Target four `READY` cards here for the current maximum of two simultaneous
delegated workers. Keep them separate from active assignments and broad
workstreams; count only bounded, independent jobs with explicit scope,
exclusions, evidence inputs, output, and completion gate. This is a 2:1
ready-task-to-authorized-worker reserve, not a promise to manufacture work when
the dependency graph does not support it.

This is a shared queue: Luna contributes asset/localization work and Claude may
contribute bounded work from the code frontier. Every card identifies its lead
and domain. The queue coordinator checks dependencies and active scopes before
marking a contribution `READY`; reserve ratios are tracked separately for each
authorized worker pool.

Current Luna/assets snapshot: 5 `READY` cards / 1 active delegated worker =
5.0; reserve / authorized capacity = 5 / 2 = 2.5. The coordinator completed
DQ-01's read-only PSMCT32 investigation; all 43 records remain raw pending
independent format or runtime evidence. Map, special-font, AFS suffix and
background-text reviews are integrated, as are the `ttlprts` and `title000`
title-art commits; combined ISO validation is complete. Each delegated task
also updates the applicable `docs/SUCCESSES.md` and/or `docs/FAILURES.md` entry
in the worker's own commit, while the coordinator maintains this shared queue
and status.

| ID / state | Lead / domain | Bounded task and exclusions | Evidence inputs | Deliverable and completion gate |
| --- | --- | --- | --- | --- |
| DQ-01 / COMPLETE (coordinator) | Luna / assets | Read-only investigation confirms all 43 PSMCT32 records share an eight-byte body shortfall across three unique payloads; exact parser and geometry limits are documented, but the cause remains unresolved. No parser, source image, or Japanese baseline was changed; keep all records raw. | `graphics/index.json`, `reports/rtx3_format_survey.json`, all 43 extracted source records, and `docs/FAILURES.md`. | [`tasks/RTX3_PSMCT32_VARIANT.md`](../tasks/RTX3_PSMCT32_VARIANT.md); all hashes and strict parse failures checked; report distinguishes observed facts from hypotheses and lists independent sample/runtime evidence needed for resolution. |
| DQ-02 / COMPLETE | Luna / assets | All 38 suffixes were checked. Their first 12 bytes admit date/time-like u16 groupings; the final u32 mirrors the member count and then alternating offset/size values. The evidence supports correlations but not semantic field names or intent. | Both pinned AFS archives, `reports/afs_inventory.json`, and [`AFS_INTERNALS.md`](../tasks/AFS_INTERNALS.md). | [`tasks/AFS_TOC_SUFFIX.md`](../tasks/AFS_TOC_SUFFIX.md) and `docs/SUCCESSES.md`; the refreshed inventory and raw suffix hashes match, and all interpretations preserve unresolved meaning. |
| DQ-03 / COMPLETE | Luna / assets | Audit of all 16 exported Japanese map textures found no Japanese words or labels; six are maze-pattern textures and ten are thumbnails marked with Arabic numerals 1–10. No overrides were authored. | `graphics/maps/*.tga`, `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_MAPS.md`](../tasks/GRAPHIC_TEXT_MAPS.md) and `docs/SUCCESSES.md`; every indexed image's name, dimensions and hash were checked, and visual/runtime limits are recorded. |
| DQ-04 / COMPLETE | Luna / assets | Both special-font atlases are 128×64 and visibly similar but not identical (2,550/8,192 RGBA pixels differ; alpha-mask IoU 99.31%). The note distinguishes mapped button/control glyphs from four unassigned glyph-like forms; runtime/codepoint lookup remains unknown. | The two tracked `graphics/text/*sp_font_jp.tga` baselines, `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/SPECIAL_FONT_ATLASES.md`](../tasks/SPECIAL_FONT_ATLASES.md) and `docs/SUCCESSES.md`; hashes, image metrics and coordinate views checked, with no source or importer changes. |
| DQ-05 / READY | Luna / assets | Compare the three BPE-decoded `.yma` menu payloads and seek evidence for a reproducible alternate structure. Read-only research: do not modify the common UI-table parser, rewrite bundles, or promote a speculative field layout. | `reports/ui_bundle_survey.json`; decoded files `extracted/assets/00039.asset/01571.decoded.bin`, `01607.decoded.bin`, and `01625.decoded.bin`, mapped in `extracted/assets/catalog.json` to `top_yma.b`, `war_common_yma.b`, and `war_stage_yma.b`; [`FAILURES.md`](FAILURES.md) and [`TASK_ASSET_WORKSPACE_METHODOLOGY.md`](TASK_ASSET_WORKSPACE_METHODOLOGY.md). | `tasks/YMA_BPE_VARIANTS.md`; compare exact sizes, hashes, headers, repeated fields/regions, and candidate table boundaries. Gate: tie any proposed structure to cross-file or independent evidence; otherwise preserve the three payloads raw and define the next evidence needed. |
| DQ-06 / COMPLETE | Luna / assets | The 46-image audit found nine obvious lettering/title/control/digit-unit surfaces, two scenes with inscription-like marks of unresolved script, and no readable writing on the other 35. No baselines or overrides changed. | `graphics/backgrounds/*.tga`, `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_BACKGROUNDS.md`](../tasks/GRAPHIC_TEXT_BACKGROUNDS.md) and `docs/SUCCESSES.md`; dimensions and hashes match all indexed rows, all 46 images reviewed, no runtime/UV claims. |
| DQ-08 / READY | Luna / assets | Audit the 27 exported Japanese effect textures for embedded Japanese labels or other translation-bearing artwork. Read-only visual/catalogue review: do not edit `_jp.tga` files or create overrides. | `graphics/effects/*.tga` (27 indexed baselines), `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | `tasks/GRAPHIC_TEXT_EFFECTS.md`; inventory all 27 filenames, dimensions and hashes, record visible text and its bounds, and distinguish lettering from particle/symbol artwork. Gate: every image is reviewed, negative findings are explicit, and no runtime/UV claims are inferred from flat textures alone. |
| DQ-09 / COMPLETE | Luna / assets | All 13 Japanese title textures without English siblings were reviewed. Ten have no visible writing, `title_mar` already uses Latin MÄR lettering, `mg_circle` has decorative non-readable symbols, and `thunder` remains ambiguous. No baselines or overrides changed. | `graphics/title/*_jp.tga` without a matching `_eng.tga` (13 indexed baselines), `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_TITLE_REMAINDERS.md`](../tasks/GRAPHIC_TEXT_TITLE_REMAINDERS.md) and `docs/SUCCESSES.md`; every dimension and source hash matches the index, all 13 reviewed, and static/runtime limits are recorded. |
| DQ-10 / READY | Luna / assets | Audit the 194 exported Japanese icon textures for embedded Japanese labels or other translation-bearing text. Read-only visual/catalogue review: do not edit `_jp.tga` files or create overrides. | `graphics/icon/*_jp.tga` (194 indexed baselines), `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | `tasks/GRAPHIC_TEXT_ICONS.md`; inventory all 194 filenames, dimensions and hashes, record any visible text and its bounds, and distinguish text from pictograms. Gate: every image is reviewed, negative findings are explicit, and no runtime/UV claims are inferred from flat textures alone. |
| DQ-11 / IN PROGRESS (`ttlprts_english_art`) | Luna / assets | Audit the 300 exported Japanese weapon textures for embedded Japanese labels or other translation-bearing artwork. Read-only visual/catalogue review: do not edit `_jp.tga` files or create overrides. | `graphics/weapons/*_jp.tga` (300 indexed baselines), `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | `tasks/GRAPHIC_TEXT_WEAPONS.md`; inventory all 300 filenames, dimensions and hashes, record visible text and bounds, and distinguish lettering from weapon/ÄRM artwork. Gate: every image is reviewed, negative findings are explicit, and no runtime/UV claims are inferred from flat textures alone. |
| DQ-12 / READY | Luna / assets | Audit the 189 exported Japanese environment textures for embedded Japanese labels, signs, or other translation-bearing artwork. Read-only visual/catalogue review: do not edit `_jp.tga` files or create overrides. | `graphics/environments/*_jp.tga` (189 indexed baselines), `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | `tasks/GRAPHIC_TEXT_ENVIRONMENTS.md`; inventory all 189 filenames, dimensions and hashes, record any visible text and its bounds, and distinguish writing from scenery/symbols. Gate: every image is reviewed, negative findings are explicit, and no runtime/UV claims are inferred from flat textures alone. |
| DQ-13 / READY | Luna / assets | Audit the 672 exported Japanese card textures for embedded Japanese labels, card titles, or other translation-bearing artwork. Read-only visual/catalogue review: do not edit `_jp.tga` files or create overrides. | `graphics/cards/*_jp.tga` (672 indexed baselines), `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | `tasks/GRAPHIC_TEXT_CARDS.md`; inventory all 672 filenames, dimensions and hashes, record visible text and bounds, and distinguish lettering from illustration/symbols. Gate: every image is reviewed, negative findings are explicit, and no runtime/UV claims are inferred from flat textures alone. |
| DQ-07 / READY | Claude / code | Characterize the common byte structure of the boot ELF's 497 `__tf*` (compiler-synthesized type-info/RTTI) linkonce sections, discovered via the linkonce cluster census but not investigated by that work. Read-only research: do not add any `__tf*` section to `config/camera_sections.txt` or `preserved/boot/`, and do not claim vtable layout, inheritance, or object size for any class. | `reports/linkonce_text_inventory.json` (the 497 `typeinfo`-kind entries and their size histogram, e.g. 84/88/52 bytes), `reports/SLPM_661.56;1.elf.json`, `llvm-objdump-21` disassembly of a representative sample (including `__tf7CCamera`, `__tf8CCamera2`, `__tf9CCameraMv`, whose surrounding classes already have partial layouts in `candidates/ee_camera/CCamera.h`), and the existing EE GCC 2.96 probe harness (`tools/run_ee_probe.py`) for compile experiments. | `docs/tasks/TYPEINFO_SECTIONS.md`; document observed byte layout/size classes, whether size correlates with base-class count or name length, and whether an unmodified natural C++ candidate reproduces at least one representative section through the existing probe harness. Gate: every structural claim is tied to disassembly evidence across multiple examples, not one; unresolved cases are left explicitly open. |

The `ttlprts` and repeated `title000` overrides, task notes, and local/ISO
round-trip evidence are integrated in [status](STATUS.md) and the graphics
localization task. No title-art worker is active; the remaining localization
audits are tracked as the bounded cards above.

| Priority | Task | State / next evidence | Completion gate |
| --- | --- | --- | --- |
| P0 | Full boot ELF reconstruction | Bootstrap baseline implemented; 2,864 object bytes plus 3,442,340 raw bytes | Full `cmp`, isolated source-only rebuild, regression tests |
| P1 | Recover trivial linkonce accessor cluster | Census done (`reports/linkonce_text_inventory.json`): 1,694 named sections/71,932 bytes/497 classes; 358 sections/38 classes recovered so far (added CFade, CBgCtrl, CGameCntrlGm, CMotionPMS, CPDataGef), 375 same-shape trivial 8-byte sections remain candidates (133 in safe gameplay classes, 238 still deliberately excluded as UI/texture/model/movie-adjacent) | Same method per section: disassemble, natural one-line accessor, `compare_sections.py`, full `cmp` |
| P1 | Broaden compiler identification | EE GCC 2.96 now matches 358 sections across 38 classes (const methods, pointer-return and pointer-argument stubs, fixed-return and no-op methods, a float pass-through, a by-value class parameter, self-referencing pointers, enum-typed parameters, a T-back-reference-compressed repeated parameter with the exact index rule confirmed, a 16-bit `short` load, 64-bit `unsigned long` fields via `ld`/`sd`, a `const char*` parameter), unmodified `-O2` | Representative nontrivial C++ methods/callers (struct-return, matrix copies), defensible common ABI/flags; no per-function steering |
| P1 | Recover CCamera/CCamera2/CCameraMv class evidence | Partial layout candidates for 3 classes exist; [GetViewRect probe](tasks/VIEW_RECT_PROBE.md) differs (40 vs 24 bytes, unaligned vs aligned copies). Next inspect callers/writes for member type/alignment evidence; matrix pairs and RTTI remain open | Justified layout/signatures plus authentic-source review and complete ELF comparison |
| P1 | Reduce main ELF raw regions | 3,442,340 bytes remain raw across many small intervals (was 3,445,140 at initial bootstrap); split further by evidenced sections as recovery proceeds | Full byte equality and provenance for each replacement |
| P1 | Decode and localize UI graphics | All 724 menu `.b` leaves decode to declared sizes. Of these, 721 use a validated 32-byte resource table: 3,084 members were extracted and all 721 untouched bundles rebuild byte-exactly. `graphics/` indexes all 30,397 catalogued TXCs; 239,444,320/239,584,968 TXC bytes have editable TGA exports (99.9413% by TXC bytes; 30,354/30,397 records). The remaining 140,648 bytes are 43 short PSMCT32 records. Current full-payload editability is B/Y=888,328,887/1,303,947,016 (68.1261%), including 615,212,537 editable movie-video bytes and 33,583,536 YOBJ coordinate bytes. All 30,354 editable `_jp.tga` baselines and the graphics index are version-controlled; Japanese baselines remain unchanged. Twelve same-size `_eng.tga` siblings override title and UI art in `mar_eng.iso`. The latest export/stage audit found twelve overrides, 43 unresolved records and zero modified Japanese TGAs. `make verify-graphics-image` reparses and byte-compares all twelve staged English TXCs from the ISO (1,378,368 bytes). Synthetic English reinsertion tests cover nested UI-table/BPE/PAC and standalone TXC/PAC paths. The literal-identity BPE writer grows edited `title_tex.b` from 301,071 to 922,091 bytes. All 2,328 direct and nested AT3 resources now parse 26,798 named-node envelopes and rebuild byte-exactly; their 192-byte and repeated 112-byte regions, UV meanings, additional title variants and runtime display remain unresolved. See [graphic localization evidence](../tasks/GRAPHIC_TEXT_LOCALIZATION.md), [title rendering evidence](../tasks/TITLE_TEXTURE_RENDERING.md), [the readable census](../reports/asset_recovery_census.md), and [census data](../reports/asset_recovery_census.json). | Validate current title/`windisp` UV and runtime display; continue the queued corpus audit for Japanese text; investigate the 43 short PSMCT32 records and alternate `.yma` structures |
| P2 | Inventory DVP overlays and IOP modules | Inventory evidence is in `reports/dvp_overlay_inventory.json`, `tasks/DVP_OVERLAY_INVENTORY.md`, and `tasks/IOP_MODULE_INVENTORY.md`: 40 DVP overlay sections (32,232 body bytes) map one-to-one to 40 12-byte table records; 14 standalone IRX reports and 16 IOPRP module records are catalogued, with an exact no-op IOPRP reconstruction. Loader semantics, custom fixups, and module behavior remain unresolved. | Correlate overlay instructions with loader/fixup behavior and module exports/callers; preserve unknown fields; validate execution/runtime before semantic claims |
| P2 | Inventory AFS/YFS/IOPRP internals | AFS TOC inventory is implemented and tested for both archives in `tasks/AFS_INTERNALS.md`; all 38 member starts are 2048-byte aligned and a synthetic no-op rebuild is exact. `tasks/AFS_TOC_SUFFIX.md` records the suffix's date/time-like first 12 bytes and exact member-table correlation in its final word, while semantic names and intent remain unresolved. Detailed YFS and IOPRP semantics remain open. | Establish remaining YFS/IOPRP structures and AFS suffix meaning; test malformed input, lossless rebuild, edited relocation, and runtime behavior |
| P1 | Asset workspace and translation surfaces | Full-disc unchanged rebuild matches; tracked catalogues cover `_msg.dat`, `CardList.txt`, and `DataBase.txt`. UI `.b` wrappers decode; 721 resource tables and 3,084 members have exact no-op reassembly and edited growth/repoint support; 3 alternate `.yma` layouts remain raw. The root `graphics/` index covers all 30,397 catalogued TXCs. The census separates Z/Y=86.8157%, A/Y=100%, B/Y=68.1261%, and C/Y=0%; Y=1,303,947,016 and Y-B=415,618,129 bytes, with byte-weighted categories in the report. All 13 SofDec movies export as immutable Japanese MPEG-2/ADX streams; profile-compatible video overrides remux with preserved ADX and CRI tags, and decoded no-op comparisons pass. B counts only 615,212,537 video elementary-stream bytes; movie audio/container overhead remain in Y-B. A disposable full mod-ISO build with a test clip re-parsed the changed movie and preserved its original ADX; emulator playback is pending. Japanese `*_jp.tga` baselines pair with English `_eng.tga` siblings. Nested and standalone graphics override tests cover reinsertion; title atlas drafts reparse through ISO/YFS/BPE/UI tables. The YOBJ editor covers XYZ coordinates only. `make build-mod-disc` writes `mar_eng.iso` in the workspace root. | Review CardList names/titles and the current title/UI graphic drafts; validate localized line layout and movie playback in an emulator; investigate database conditions, AT node properties, remaining YOBJ fields, alternate `.yma` layouts, append placement; see the [CardList review](../tasks/CARDLIST_TRANSLATION_REVIEW.md), [asset method](TASK_ASSET_WORKSPACE_METHODOLOGY.md), [readable census](../reports/asset_recovery_census.md), and [census data](../reports/asset_recovery_census.json) |
| P1 | Decode YOBJ/YMP model geometry | `tools/yobj.py` validates 899 direct YMPs (197,085,308 bytes) plus 14 nested UI-bundle YMPs; all 913 parse and rebuild byte-exactly, with 807,439 POF0 pointer slots audited. `tools/yobj_geometry.py` exports and same-size rebuilds 9,976 meshes / 1,399,314 vertices, exposing 33,583,536 editable XYZ position/normal source bytes. A synthetic nested edit survives UI-table/BPE/PAC reinsertion. Total B/Y is 68.1261%; other model fields and runtime semantics remain unvalidated, and internal growth is unsupported. YPC files remain unresolved. | Correlate coordinate meaning with the game draw path/runtime; recover topology/UVs and other model fields; establish POF0 fixups before internal growth; test changed YMP through the full PAC/YFS/ISO path and runtime; see [YOBJ evidence](../tasks/YOBJ_MODEL_RECOVERY.md) |
| P2 | Full-disc reconstruction and relocation | Untouched image matches exactly. Current translated image is 5,023,174,656 bytes (SHA-256 `b42048452f8d8b41497fff87f4fa5e5f15f983529d179012e6a868482494f5a0`); all twelve staged English TXCs (1,378,368 bytes) reparse byte-exactly, and the pinned-reference comparison authenticates the baserom and reports 757,730,801 differing bytes in `reports/mar_eng_compare_12_graphics.json`. Append placement carries the rebuilt YFS; `make build-mod-disc` atomically replaces an existing ISO only after success. See [graphic localization evidence](../tasks/GRAPHIC_TEXT_LOCALIZATION.md). | Runtime acceptance, broader relocation cases and a more space-efficient placement strategy |
| P3 | Independent provenance / runtime checks | Not performed | External dump provenance and recorded runtime validation |

Completed enabling work: ISO/ELF inventory, lossless asset export/rebuild and
streaming whole-disc comparison, selected assembly preservation,
historical compiler probe, explicit boot reconstruction, isolated rebuild check,
mandatory commit attribution trailers, and a linkonce-cluster census tool.

Any source promotion must satisfy authenticity independently of byte equality.
Do not treat P0 bootstrap completion as recovered-code or whole-game completion.
