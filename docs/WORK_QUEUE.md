# Work queue

Authority: [STANDARDS.md](STANDARDS.md). Updated: 2026-09-24.
Manager maintains this page and [status](STATUS.md) at each verified milestone.

## Delegation-ready reserve

Target four `READY` cards here for the current maximum of two simultaneous
delegated workers. Keep them separate from active assignments and broad
workstreams; count only bounded, independent jobs with explicit scope,
exclusions, evidence inputs, output, and completion gate. This is a 2:1
ready-task-to-authorized-worker reserve, not a promise to manufacture work when
the dependency graph does not support it.

This is a shared queue: Luna contributes asset/localization work and Claude may
contribute bounded work from the code frontier. Every card identifies its lead
and domain. The queue coordinator checks dependencies and active scopes before
marking a contribution `READY`; reserve ratios are tracked separately for each
authorized worker pool.

Current Luna/assets snapshot: 5 `READY` cards / 0 active delegated workers.
Delegated work is paused at the owner's direction, so a reserve/capacity ratio
is not currently meaningful. DQ-17
(`password_tex.b`) and DQ-19 (`top_tex.b`) are complete and integrated as
`763807b` and `4778c17`. DQ-24 (`winner_tex.b`) and DQ-25
(`war_sel_base_tex.b`) are complete and integrated as `23b4937` and `e23f4da`.
DQ-26 (`war_common_tex.b`) is complete and integrated as `2025d11`; DQ-27
(`shop_tex.b`) and DQ-28 (`lib_main_tex.b`) are complete and integrated as
`29cecee` and `3a997a4`. DQ-21 (`lib_movie_sum.b`) is complete and integrated
as `40ed02f`; DQ-22 (`train_tex.b`) is complete and integrated as `6ccd26a`;
DQ-23 (`GameOver_tex.b`) is complete and integrated as `ab3b437`; DQ-29
(`trans_tex.b`) is complete and integrated as `53be1d4`. DQ-34's `TRAINING`
texture and DQ-35's `ARM SET`/`RANDOM` labels are integrated as `f614b19` and
`747f4a4`, respectively; both await lead RTX3/ISO validation. DQ-31 (the
single-texture TGS menu audit) is integrated as `2ea24f0`; source worker commit
`23a5198` has identical content and author identity. DQ-36's 13-row
remaining-menu audit is integrated as `8ff964d`. DQ-37 (the `ｗ.b` bundle
audit) is integrated as `df4315d`; source worker commit `3955ac5` has identical
content and author identity.
DQ-20's read-only audit is complete;
DQ-30's `OPTIONS` translation and DQ-38's title-logo rework are active
coordinator work.
DQ-13 and DQ-14 through DQ-16 are complete; the current English ISO reparses
all 14 graphics and all three text catalogues byte-exactly. DQ-18 is complete
with the corrected Claude code commit and both DQ-18 follow-ups integrated.
DQ-32 is paused at the owner's direction; its staged, uncommitted save/load
audit is preserved in `/mnt/c/users/name1ess/documents/decomps/marps2-dq32-saveload-audit`
and must be reconciled before reassignment. DQ-33's war-close audit is complete
and integrated as `cda3366` (worker source `3d0e7a8`). DQ-40
is integrated as `743ec3e` (worker source `d0be870`); its 16 character portraits
contain no readable text. DQ-41 is integrated as `d1aedad` (worker source
`dafa268`); its 16 portraits contain tiny unresolved balloon-like marks but no
readable labels. Both source authors and trailers are preserved on integration.
DQ-39, DQ-42, DQ-43, and DQ-44 remain ready. Each worker records task evidence
and a scoped success/failure; the coordinator maintains shared queue/status,
resolves integration collisions, and validates the combined ISO.
Branch reconciliation found no missing DQ-11 or DQ-18 content: the weapon
audit's task report has the same blob in the current tree as its side commit,
and the Claude RTTI task reports are also identical to their current-tree blobs;
their findings and integration commits are already recorded. Do not add
duplicate cherry-picks solely because the original side commits are not
ancestors of `master`. The pre-correction backup branch is retained only as
history and is not a candidate for integration.

| ID / state | Lead / domain | Bounded task and exclusions | Evidence inputs | Deliverable and completion gate |
| --- | --- | --- | --- | --- |
| DQ-01 / COMPLETE (coordinator) | Luna / assets | Read-only investigation confirms all 43 PSMCT32 records share an eight-byte body shortfall across three unique payloads; exact parser and geometry limits are documented, but the cause remains unresolved. No parser, source image, or Japanese baseline was changed; keep all records raw. | `graphics/index.json`, `reports/rtx3_format_survey.json`, all 43 extracted source records, and `docs/FAILURES.md`. | [`tasks/RTX3_PSMCT32_VARIANT.md`](../tasks/RTX3_PSMCT32_VARIANT.md); all hashes and strict parse failures checked; report distinguishes observed facts from hypotheses and lists independent sample/runtime evidence needed for resolution. |
| DQ-02 / COMPLETE | Luna / assets | All 38 suffixes were checked. Their first 12 bytes admit date/time-like u16 groupings; the final u32 mirrors the member count and then alternating offset/size values. The evidence supports correlations but not semantic field names or intent. | Both pinned AFS archives, `reports/afs_inventory.json`, and [`AFS_INTERNALS.md`](../tasks/AFS_INTERNALS.md). | [`tasks/AFS_TOC_SUFFIX.md`](../tasks/AFS_TOC_SUFFIX.md) and `docs/SUCCESSES.md`; the refreshed inventory and raw suffix hashes match, and all interpretations preserve unresolved meaning. |
| DQ-03 / COMPLETE | Luna / assets | Audit of all 16 exported Japanese map textures found no Japanese words or labels; six are maze-pattern textures and ten are thumbnails marked with Arabic numerals 1–10. No overrides were authored. | `graphics/maps/*.tga`, `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_MAPS.md`](../tasks/GRAPHIC_TEXT_MAPS.md) and `docs/SUCCESSES.md`; every indexed image's name, dimensions and hash were checked, and visual/runtime limits are recorded. |
| DQ-04 / COMPLETE | Luna / assets | Both special-font atlases are 128×64 and visibly similar but not identical (2,550/8,192 RGBA pixels differ; alpha-mask IoU 99.31%). The note distinguishes mapped button/control glyphs from four unassigned glyph-like forms; runtime/codepoint lookup remains unknown. | The two tracked `graphics/text/*sp_font_jp.tga` baselines, `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/SPECIAL_FONT_ATLASES.md`](../tasks/SPECIAL_FONT_ATLASES.md) and `docs/SUCCESSES.md`; hashes, image metrics and coordinate views checked, with no source or importer changes. |
| DQ-05 / COMPLETE | Luna / assets | All three BPE-decoded `.yma` resources match their stored decode and sibling `.pac` byte-for-byte. Shared `0x20`/`root` header clues and YANM-to-POF0 extents are recorded as boundary evidence only; the alternate grammar remains unresolved. | `reports/ui_bundle_survey.json`, the three decoded sidecars, mapped `.b` members and sibling `.pac` files; [`FAILURES.md`](FAILURES.md). | [`tasks/YMA_BPE_VARIANTS.md`](../tasks/YMA_BPE_VARIANTS.md); exact sizes/hashes, headers, tags and extent checks verified; no parser or raw payload changes. Commit `4ae34ec` integrated as `b64984e`. |
| DQ-06 / COMPLETE | Luna / assets | The 46-image audit found nine obvious lettering/title/control/digit-unit surfaces, two scenes with inscription-like marks of unresolved script, and no readable writing on the other 35. No baselines or overrides changed. | `graphics/backgrounds/*.tga`, `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_BACKGROUNDS.md`](../tasks/GRAPHIC_TEXT_BACKGROUNDS.md) and `docs/SUCCESSES.md`; dimensions and hashes match all indexed rows, all 46 images reviewed, no runtime/UV claims. |
| DQ-07 / COMPLETE | Claude / code | Characterized the boot ELF's 498 `__tf*` (compiler-synthesized type-info/RTTI) linkonce sections: two/three construct-on-first-use code shapes (root, single-base, one 92-byte outlier), size (52/64/80/84/88/92) shown to track shared-helper choice and address-encoding coincidence rather than base-class count, and 440 single-base class-to-base-class edges decoded directly from each section's own `jal` instructions. No `__tf*` section was added to `config/camera_sections.txt` or `preserved/boot/`; no vtable layout or object size was claimed. | `reports/linkonce_text_inventory.json`, `reports/typeinfo_hierarchy.json` (generated by `tools/typeinfo_hierarchy.py` and covered by four new tests), and `llvm-objdump-21` cross-validation of all 498 sections' `jal` targets (938 instructions, zero mismatches). | [`docs/tasks/TYPEINFO_SECTIONS.md`](../docs/tasks/TYPEINFO_SECTIONS.md) and `docs/CODE_SUCCESSES.md`/`docs/CODE_FAILURES.md`; every structural claim is tied to disassembly evidence across multiple examples, both confirmed base-class spot checks (`CCamera`->`C3dObject`, `CCamera2`->`CCamera`) match the existing partial layouts in `candidates/ee_camera/CCamera.h`, and the `CPrimList` outlier and the shared runtime-helper identity are left explicitly open. Claude commit `a4a54d3` integrated as `d6b0d63`. |
| DQ-08 / COMPLETE | Luna / assets | All 27 reviewed effects are flame, lightning, flare, gradient, or frame-like art; none contains readable Japanese or English wording, digits, or other text. Six exact duplicate groups cover 18 unique TGA hashes. No baselines or overrides changed. | `graphics/effects/*.tga` (27 indexed baselines), `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_EFFECTS.md`](../tasks/GRAPHIC_TEXT_EFFECTS.md) and `docs/SUCCESSES.md`; all dimensions and hashes match the index, all images were reviewed, and static/runtime limits are recorded. |
| DQ-09 / COMPLETE | Luna / assets | All 13 Japanese title textures without English siblings were reviewed. Ten have no visible writing, `title_mar` already uses Latin MÄR lettering, `mg_circle` has decorative non-readable symbols, and `thunder` remains ambiguous. No baselines or overrides changed. | `graphics/title/*_jp.tga` without a matching `_eng.tga` (13 indexed baselines), `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_TITLE_REMAINDERS.md`](../tasks/GRAPHIC_TEXT_TITLE_REMAINDERS.md) and `docs/SUCCESSES.md`; every dimension and source hash matches the index, all 13 reviewed, and static/runtime limits are recorded. |
| DQ-10 / COMPLETE | Luna / assets | Review of all 194 icon textures found 29 text-bearing appearances across eight unique images/hashes; the rest are pictograms, controls, numbers, or duplicated appearances. No baselines or overrides changed. | `graphics/icon/*_jp.tga` (194 indexed baselines), `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_ICONS.md`](../tasks/GRAPHIC_TEXT_ICONS.md); all rows and hashes checked, visible text bounds recorded, and runtime/UV use left unclaimed. Commit `1de01b9` integrated as `3925538`. |
| DQ-11 / COMPLETE | Luna / assets | All 300 weapon images were reviewed. One confirmed Japanese command says `ARMを装着する` (“Equip ARM”); English `GET` and `ARM START` and a controller-glyph atlas are already present. Two color-coded slot-label strips remain uncertain candidates. No baselines or overrides changed. | `graphics/weapons/*_jp.tga` (300 indexed baselines), `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_WEAPONS.md`](../tasks/GRAPHIC_TEXT_WEAPONS.md) and `docs/SUCCESSES.md`; all names, dimensions and hashes match, all images were visually reviewed, and text/runtime limits are recorded. |
| DQ-12 / COMPLETE | Luna / assets | Review of 189 environment textures found five text-bearing candidates, including one readable Japanese label; glyph-like gate marks and two image-like signs remain unresolved. No baselines or overrides changed. | `graphics/environments/*_jp.tga` (189 indexed baselines), `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_ENVIRONMENTS.md`](../tasks/GRAPHIC_TEXT_ENVIRONMENTS.md); all rows and hashes checked, writing distinguished from scenery, and runtime/UV use left unclaimed. Commit `780fb1e` integrated as `2bdb26b`. |
| DQ-13 / COMPLETE | Luna / assets | Audit the 672 exported Japanese card textures for embedded Japanese labels, card titles, or other translation-bearing artwork. Read-only visual/catalogue review: do not edit `_jp.tga` files or create overrides. | `graphics/cards/*_jp.tga` (672 indexed baselines), `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | `tasks/GRAPHIC_TEXT_CARDS.md`; inventory all 672 filenames, dimensions and hashes, record visible text and bounds, distinguish lettering from illustrations/symbols, and record a scoped success/failure result. Every image is reviewed; no runtime/UV inference. Worker commit `c75d486` integrated as `e653d63`. |
| DQ-14 / COMPLETE (coordinator) | Luna / localization | Drafted literal English for all 39 previously untranslated column-5 conditions (four phrases) in `DataBase.txt`. “Hidden flag OFF” is preserved without claiming visibility or unlock behavior. | `localization/database.json`, anchored source SHA-256 `b775dfa84567336cce820b5923f424ccea1f52e69e13e1075b45cb7f74f19f1f`, `tasks/MESSAGE_TABLE.md`, and existing `Password` markers. | [`tasks/DATABASE_CONDITION_SEMANTICS.md`](../tasks/DATABASE_CONDITION_SEMANTICS.md); all 39 occurrences/four values accounted for, catalog validates and applies to 3,684-byte CP932 output, Japanese original cells/hash unchanged. Current full ISO reparse confirms the 3,684-byte CP932 output exactly, including all 39 drafted conditions; no runtime or unlock semantics are claimed. |
| DQ-15 / COMPLETE (coordinator) | Luna / localization graphics | Create an English sibling for the confirmed 256×64 command texture `graphics/weapons/00039_00921_0004_arrm_jp.tga` (“Equip ARM”). Preserve the baseline and canvas; fit the text to measured bounds using a tracked transparent PNG and deterministic composition. | `tasks/GRAPHIC_TEXT_WEAPONS.md` row 233, `graphics/index.json`, source TGA, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_ARM_COMMAND.md`](../tasks/GRAPHIC_TEXT_ARM_COMMAND.md), `tools/graphics_compose.py`, `graphics_rules.mk`, English TGA and layer; source hash unchanged, exact reproducibility, original PSMT8 import, native-size review, the rebuilt ISO reparse confirms the 17,472-byte source-size TXC; the combined build validates all 14 overrides. No runtime/UV claim. |
| DQ-16 / COMPLETE | Luna / localization graphics | Translate three rows of Japanese control/help text in `graphics/backgrounds/00039_00991_0030_btnhlptxt_jp.tga` (128×64) as `D-PAD`, `RIGHT STICK`, and `LEFT STICK`, preserving the separate control pictogram and Japanese baseline. | `tasks/GRAPHIC_TEXT_BACKGROUNDS.md` row 23, `localization/messages.json`, `graphics/index.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_BUTTON_HELP.md`](../tasks/GRAPHIC_TEXT_BUTTON_HELP.md) and `graphics/backgrounds/00039_00991_0030_btnhlptxt_eng.tga`; native 8×16 atlas glyphs used, source/TGA/TXC hashes and palette checked, both Japanese and English TGA exports round-trip byte-exactly, and pictogram pixels remain unchanged. The combined ISO reparse confirms the English TXC exactly; no runtime/UV claim. Worker commit `27a0c5a` is patch-equivalent to already-integrated `0c1a611`. |
| DQ-17 / COMPLETE (integrated as `763807b`) | Luna / assets | Audit the 10 exported Japanese textures in `password_tex.b` for embedded Japanese labels, button/help text, or other translation-bearing artwork. Read-only review: do not edit `_jp.tga` files or create overrides. | `graphics/index.json`, `graphics/*` entries mapped to `disc!/_DATA.YFS;1!/data/menu/password_tex.b`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | `tasks/GRAPHIC_TEXT_PASSWORD_TEX.md` plus a scoped `docs/SUCCESSES.md` entry; all 10 source filenames, dimensions and full hashes, visible text/bounds and negative findings are recorded, and every image was visually reviewed. Japanese baselines remain unchanged; no UV/runtime inference. |
| DQ-18 / COMPLETE | Claude / code | Characterized the three shared MIPS helpers called by `__tf*` registration: `0x32c1f8` initializes `__user_type_info`, `0x32c1d8` initializes `__si_type_info`, and `0x32c1b0` initializes `__class_type_info`, with each identity established by its vtable pointer landing on a named linkonce data section. Also characterized `0x32c170` as `type_info::operator==` and `0x32c138` as a plausible deleting-destructor dispatch trampoline. The pinned EE GCC installation contains no bundled `.a`/`.o` runtime archive, so these helpers are not reproducible from the available toolchain; no code sections were added. | `docs/tasks/TYPEINFO_SECTIONS.md`, `reports/typeinfo_hierarchy.json`, disassembly of `0x32c100`–`0x32c220`, and toolchain archive audit. | [`docs/tasks/RTTI_RUNTIME_HELPERS.md`](../docs/tasks/RTTI_RUNTIME_HELPERS.md), `docs/CODE_SUCCESSES.md`, and `docs/WORK_QUEUE.md`; each helper's behavior and the three class identities are tied to disassembly and named-section evidence; no register pinning or flag changes used. Claude correction `6bbf961` integrated as `2f3a0af`; evidence commit `b5eb5cf` integrated as `f3ea009`, preserving Claude as author. |
| DQ-19 / COMPLETE (integrated as `4778c17`) | Luna / assets | Audit the 12 exported Japanese textures in `top_tex.b` for embedded Japanese labels, menu text, or other translation-bearing artwork. Read-only review: do not edit `_jp.tga` files or create overrides. | `graphics/index.json`, `graphics/*` entries mapped to `disc!/_DATA.YFS;1!/data/menu/top_tex.b`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | `tasks/GRAPHIC_TEXT_TOP_TEX.md` plus a scoped `docs/SUCCESSES.md` entry; all 12 source filenames, dimensions and full hashes, visible text/bounds and negative findings are recorded, and every image was visually reviewed. Japanese baselines remain unchanged; no UV/runtime inference. |
| DQ-20 / COMPLETE (coordinator) | Luna / assets | Audit the 6 not-yet-audited Japanese user-interface textures in `option_tex.b` for embedded Japanese labels, option names, or other translation-bearing artwork. Read-only review: do not edit `_jp.tga` files or create overrides. Scope is the `graphics/index.json` rows whose `asset_path` is exactly `disc!/_DATA.YFS;1!/data/menu/option_tex.b` and category is `user_interface`; exclude its icon row already reviewed in DQ-10. | The scoped 6 entries in `graphics/index.json`, their `graphics/user_interface/*_jp.tga` files, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_OPTION_TEX.md`](../tasks/GRAPHIC_TEXT_OPTION_TEX.md) and a scoped success entry; all six full TGA/TXC hashes, dimensions, TGA headers/extents, visual findings, Japanese text bounds, and unresolved tiny marks are recorded. No baseline or override changed during the audit. |
| DQ-21 / COMPLETE (integrated as `40ed02f`) | Luna / assets | Audit the 23 exported Japanese 64×64 textures in `lib_movie_sum.b` for embedded captions, menu text, or other translation-bearing artwork. Read-only review: do not edit `_jp.tga` files or create overrides. | `graphics/index.json`, `graphics/*` entries mapped to `disc!/_DATA.YFS;1!/data/menu/lib_movie_sum.b`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_LIB_MOVIE_SUM.md`](../tasks/GRAPHIC_TEXT_LIB_MOVIE_SUM.md) and scoped success entry; all 23 indexed names, dimensions, hashes, type-2 32-bit top-origin headers/extents and previews checked. No readable text found; all alpha is opaque. Japanese baselines unchanged; no UV/runtime inference. |
| DQ-22 / COMPLETE (integrated as `6ccd26a`) | Luna / assets | Audit the 11 exported Japanese textures in `train_tex.b` for embedded tutorial instructions, menu labels, or other translation-bearing artwork. Read-only review: do not edit `_jp.tga` files or create overrides. | `graphics/index.json`, `graphics/*` entries mapped to `disc!/_DATA.YFS;1!/data/menu/train_tex.b`, `localization/messages.json`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_TRAINING_TEX.md`](../tasks/GRAPHIC_TEXT_TRAINING_TEX.md) plus a scoped success entry; all 11 indexed dimensions, full image hashes, TGA headers/extents and previews checked; 36 tutorial records searched for visible terms. The `trng` title reads `トレーニング` (“Training”), with inclusive visible bounds `(1,12)–(224,57)`; the `icon` atlas clearly includes `ARM セット` and `ランダム`. Baselines unchanged; uncertain gate/window marks and runtime/UV use remain open. |
| DQ-23 / COMPLETE (integrated as `ab3b437`) | Luna / assets | Audit all 4 exported Japanese textures in `GameOver_tex.b`. The two background cards contain low-alpha English `GAME OVER` lettering, `gmover` is an opaque English wordmark, and `flear` has no text; no Japanese translation override is needed. | `graphics/index.json` records mapped to `disc!/_DATA.YFS;1!/data/menu/GameOver_tex.b`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_GAMEOVER_TEX.md`](../tasks/GRAPHIC_TEXT_GAMEOVER_TEX.md) and scoped success entry; all 4 images, TGA hashes/dimensions/headers/extents and alpha-plane findings documented; Japanese baselines unchanged, no UV/runtime inference. |
| DQ-24 / COMPLETE (integrated as `23b4937`) | Luna / assets | Audit the 22 Japanese user-interface texture rows from `winner_tex.b` for winner-screen text, labels, or other translation-bearing artwork. Read-only review: do not edit `_jp.tga` files or create overrides. Scope is the `graphics/index.json` rows whose `asset_path` is exactly `disc!/_DATA.YFS;1!/data/menu/winner_tex.b` and category is `user_interface`, excluding `0025_windisp` already covered by existing localization evidence and all non-UI bundle members. | The scoped 22 entries in `graphics/index.json`, their `graphics/user_interface/*_jp.tga` files, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | `tasks/GRAPHIC_TEXT_WINNER_TEX.md` plus a scoped `docs/SUCCESSES.md` entry; all 22 names, dimensions and hashes, three Japanese-looking label rows, and ambiguous marks are recorded; seven unique raster hashes were checked. Japanese baselines remain unchanged; no UV/runtime inference. |
| DQ-25 / COMPLETE (integrated as `e23f4da`) | Luna / assets | Audit the 17 Japanese user-interface texture rows from `war_sel_base_tex.b` for character labels, selection text, or other translation-bearing artwork. Read-only review: do not edit `_jp.tga` files or create overrides. Scope is the `graphics/index.json` rows whose `asset_path` is exactly `disc!/_DATA.YFS;1!/data/menu/war_sel_base_tex.b` and category is `user_interface`; exclude its two non-UI members. | The scoped 17 entries in `graphics/index.json`, their `graphics/user_interface/*_jp.tga` files, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | `tasks/GRAPHIC_TEXT_WAR_SEL_BASE_TEX.md` plus a scoped `docs/SUCCESSES.md` entry; all filenames, dimensions and hashes, visual findings, and 16 exact duplicate rasters are recorded. Japanese baselines remain unchanged; no UV/runtime inference. |
| DQ-26 / COMPLETE (integrated as `2025d11`) | Luna / assets | Audit the 13 Japanese user-interface texture rows from `war_common_tex.b` for menu labels or other translation-bearing artwork. Read-only review: do not edit `_jp.tga` files or create overrides. Scope is the `graphics/index.json` rows whose `asset_path` is exactly `disc!/_DATA.YFS;1!/data/menu/war_common_tex.b` and category is `user_interface`. | The scoped 13 entries in `graphics/index.json`, their `graphics/user_interface/*_jp.tga` files, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | `tasks/GRAPHIC_TEXT_WAR_COMMON_TEX.md` plus a scoped `docs/SUCCESSES.md` entry; all names, dimensions, hashes, TGA headers/extents, visible bounds and unresolved marks are recorded. Japanese baselines remain unchanged; no UV/runtime inference. |
| DQ-27 / COMPLETE (integrated as `29cecee`) | Luna / assets | Audit the 6 Japanese user-interface texture rows from `shop_tex.b` for shop commands, labels, or other translation-bearing artwork. Read-only review: do not edit `_jp.tga` files or create overrides. Scope is the `graphics/index.json` rows whose `asset_path` is exactly `disc!/_DATA.YFS;1!/data/menu/shop_tex.b` and category is `user_interface`. | The scoped 6 entries in `graphics/index.json`, their `graphics/user_interface/*_jp.tga` files, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_SHOP_TEX.md`](../tasks/GRAPHIC_TEXT_SHOP_TEX.md) and scoped `docs/SUCCESSES.md` entry; all six names, dimensions, hashes, headers, extents, visual findings, and the excluded subtitle row are recorded. Japanese baselines unchanged; no UV/runtime inference. |
| DQ-28 / COMPLETE (integrated as `3a997a4`) | Luna / assets | Audit the 5 Japanese user-interface texture rows from `lib_main_tex.b` for library/menu labels or other translation-bearing artwork. Read-only review: do not edit `_jp.tga` files or create overrides. Scope is the `graphics/index.json` rows whose `asset_path` is exactly `disc!/_DATA.YFS;1!/data/menu/lib_main_tex.b` and category is `user_interface`. | The scoped 5 entries in `graphics/index.json`, their `graphics/user_interface/*_jp.tga` files, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_LIB_MAIN_TEX.md`](../tasks/GRAPHIC_TEXT_LIB_MAIN_TEX.md) plus scoped `docs/SUCCESSES.md` entry; all five full hashes, dimensions, headers/extents and visual reviews are recorded. `lbry` appears to read `ライブラリ`, `plt_001` appears to read `キャラの戦歴` and carries `014-007`; `window` marks remain unresolved. Baselines unchanged; no UV/runtime claim. |
| DQ-29 / COMPLETE (integrated as `53be1d4`) | Luna / assets | Audit the 3 Japanese user-interface texture rows from `trans_tex.b`. None has confidently readable Japanese or English; the circular emblem's irregular rune-like ring marks remain unresolved. | The exact 3 `graphics/index.json` rows for `disc!/_DATA.YFS;1!/data/menu/trans_tex.b`, their mapped TGA images, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_TRANS_TEX.md`](../tasks/GRAPHIC_TEXT_TRANS_TEX.md) plus scoped success entry; names, hashes, dimensions, TGA headers/extents and visual review recorded. No baselines/overrides changed; no UV/runtime inference. |
| DQ-30 / IN PROGRESS (coordinator) | Luna / assets | Translate the confirmed `オプション` wordmark in `00039_01544_0003_optn_jp.tga` as `OPTIONS` using a tracked transparent layer and deterministic composition into the exact measured 188×46-pixel text bounds. Add a tested aspect-changing fit mode only for explicitly measured bands; preserve the rest of the Japanese baseline. Exclude the other five audited textures and the icon row. | DQ-20 audit evidence, the immutable Japanese TGA and its index/TXC hashes, `tools/graphics_compose.py`, and the RTX3 importer. | `tasks/GRAPHIC_TEXT_OPTIONS_LOCALIZATION.md`, a scoped success/failure entry, tracked `*_eng_layer.png` and `_eng.tga`; compositor tests; source-palette import; rebuilt `mar_eng.iso`; `make verify-graphics-image` must reparse the English TXC exactly. Runtime UV and emulator acceptance remain open. |
| DQ-31 / COMPLETE (integrated as `2ea24f0`; worker source `23a5198`) | Luna / assets | Audited the sole Japanese `user_interface` texture row from `TGS_menu_tex.b`; it contains `1月3日まで!!` and `あと3日間!!`, plus diffuse repeated marks documented as likely echoes. | The exact `graphics/index.json` row for `disc!/_DATA.YFS;1!/data/menu/TGS_menu_tex.b` and its mapped TGA. | [`tasks/GRAPHIC_TEXT_TGS_MENU_TEX.md`](../tasks/GRAPHIC_TEXT_TGS_MENU_TEX.md) and scoped success evidence; dimensions, hash, TGA header/extent and visual review recorded. Baseline preserved; no UV/runtime claim. Worker commit `23a5198` is patch-equivalent to integrated commit `2ea24f0`, with the same worker author and trailers. |
| DQ-32 / PAUSED (uncommitted work preserved) | Luna / assets | Audit the single Japanese `user_interface` texture row from `saveload_tex.b` for save/load labels or other translation-bearing artwork. Read-only review: do not edit `_jp.tga` or create overrides. | The exact `graphics/index.json` row whose `asset_path` is `disc!/_DATA.YFS;1!/data/menu/saveload_tex.b`, its mapped `graphics/user_interface/*_jp.tga`, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). Existing staged work is in `/mnt/c/users/name1ess/documents/decomps/marps2-dq32-saveload-audit` at its own worker branch; preserve it until reconciled with current master. | `tasks/GRAPHIC_TEXT_SAVELOAD_TEX.md` plus a scoped `docs/SUCCESSES.md` or `docs/FAILURES.md` entry inserted after `## Password-menu textures separate Japanese labels from entry glyphs and frame parts`; record the filename, dimensions, full hash, TGA header/extent, visible text/bounds or negative finding, and visual review. Preserve Japanese baseline; make no UV/runtime inference. |
| DQ-33 / COMPLETE (integrated as `cda3366`; worker source `3d0e7a8`) | Luna / assets | Audited the sole Japanese user-interface texture exported from `war_close_tex.b`: a pale 32×32 selection frame with transparent center and no visible text. | The exact `graphics/index.json` row for `disc!/_DATA.YFS;1!/data/menu/war_close_tex.b` (`sel_frame`) and its mapped `graphics/user_interface/00039_01602_0000_sel_frame_jp.tga`. | [`tasks/GRAPHIC_TEXT_WAR_CLOSE_TEX.md`](../tasks/GRAPHIC_TEXT_WAR_CLOSE_TEX.md) and scoped success evidence; TGA SHA-256, dimensions, header and 4,114-byte extent verified, image visually reviewed. Japanese baseline preserved; no UV/runtime/ISO claim. Worker author and trailers retained. |
| DQ-34 / LEAD VALIDATION (integrated as `f614b19`) | Luna / localization graphics | Create an English sibling for the confirmed `トレーニング` title in `graphics/user_interface/00039_01593_0010_trng_jp.tga`, using the exact text `TRAINING`. Keep the 256×64 canvas and Japanese source immutable; translate only the measured title bounds, without changing other training-menu textures. | DQ-22 evidence in [`GRAPHIC_TEXT_TRAINING_TEX.md`](../tasks/GRAPHIC_TEXT_TRAINING_TEX.md), its indexed TGA hash, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | `tasks/GRAPHIC_TEXT_TRAINING_TITLE.md`, scoped success evidence, tracked transparent `*_eng_layer.png` and `_eng.tga`; layer/output hashes, measured bounds and aspect-preserving fit documented, with exact deterministic output and zero changes outside the clear region. Lead still verifies source-palette RTX3 import and ISO reparse. No UV/runtime claim. |
| DQ-35 / LEAD VALIDATION (integrated as `747f4a4`) | Luna / localization graphics | Create English labels for the two clearly readable Japanese strings `ARM セット` → `ARM SET` and `ランダム` → `RANDOM` in `graphics/icon/00039_01593_0008_icon_jp.tga`. Preserve the 256×256 canvas, Japanese baseline, controller symbols, Latin control labels, and all other unconfirmed text; do not alter any other training-menu texture. | DQ-22 evidence in [`GRAPHIC_TEXT_TRAINING_TEX.md`](../tasks/GRAPHIC_TEXT_TRAINING_TEX.md), the indexed TGA hash, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`GRAPHIC_TEXT_TRAINING_ICON_LOCALIZATION.md`](../tasks/GRAPHIC_TEXT_TRAINING_ICON_LOCALIZATION.md), scoped success evidence, tracked transparent layer and `_eng.tga`; exact deterministic composition and zero changes outside measured label rectangles verified by the worker. Lead palette import and ISO reparse remain pending. Other tiny phrases, UV/runtime use, and emulator acceptance remain open. |
| DQ-36 / COMPLETE (integrated as `8ff964d`) | Luna / assets | Audited 13 Japanese `user_interface` texture rows in seven remaining menu assets: `esy_menu.tex`, `war_deci_01_tex.b`, `war_deci_02_tex.b`, `war_dialog_tex.b`, `war_pad_tex.b`, `war_sel_act_tex.b`, and `wgm_card7_txc.b`. The title000 mark/credits are Japanese despite an existing English sibling; three outlined `window` rows and small center-bar marks remain unresolved. No baselines or overrides changed. | The exact `graphics/index.json` rows under those seven `disc!/_DATA.YFS;1!/data/menu/` asset paths and category `user_interface`, their mapped `_jp.tga` images, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_REMAINING_MENU_TEX.md`](../tasks/GRAPHIC_TEXT_REMAINING_MENU_TEX.md) and scoped success evidence; all 13 filenames, dimensions, full hashes, headers/extents and visual findings recorded; Japanese baseline/index parity checked; no UV/runtime inference. |
| DQ-37 / COMPLETE (integrated as `df4315d`; worker source `3955ac5`) | Luna / assets | Audited all 30 Japanese `user_interface` textures exported from the full-width-name bundle `ｗ.b`. Confirmed paired field-name labels and `サイコロ操作`; `silver_dice`, `window`, `wrgm`, and center-bar candidates remain unresolved. | The exact 30 `graphics/index.json` rows for `disc!/_DATA.YFS;1!/data/menu/ｗ.b`, their mapped TGA images, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_W_MENU_TEX.md`](../tasks/GRAPHIC_TEXT_W_MENU_TEX.md) and scoped success evidence; filenames, hashes, dimensions, headers/extents and visual findings are recorded. Japanese baselines preserved; no UV/runtime inference. Worker commit `3955ac5` is patch-equivalent to integrated commit `df4315d`, with the same worker author and trailers. |
| DQ-38 / IN PROGRESS (coordinator; art and ISO reparse passed) | Luna / localization graphics | Replace the subtitle-derived lettering that was misidentified as MÄR HEAVEN. Rebuild `00039_01559_0003_title`, `00039_01565_0000_ttlprts`, and `00039_01566_0002_title_marh`, plus dependent standalone `00039_00985_00000_title000`, from the tracked reference crops. Preserve the separate `ARM FIGHT DREAM` subtitle and all Japanese baselines. Static composites are staged in the root `mar_eng.iso`; emulator display/UV review remains open. | Four indexed `_jp.tga` baselines and existing English siblings, `graphics/title/mar_heaven_logo_source.png`, `graphics/title/mar_heaven_wordmark_source.png`, `graphics/index.json`, title rendering evidence, and owner-supplied `T_TTL06.KCG_en.png` reference (SHA-256 `33cbe51d2ac9c5fa409ca023ab9147f67a78304ca9f25328932540b51643e947`). | [`tasks/GRAPHIC_TEXT_TITLE_LOGO_REWORK.md`](../tasks/GRAPHIC_TEXT_TITLE_LOGO_REWORK.md), scoped failure/success findings, tracked editable source crops/layers, and deterministic composition. All 126 tests passed; `make build-mod-disc` and `make verify-graphics-image` passed with all four title TXCs reparse-identical. Do not claim runtime, UV, or draw-order correctness until the English title is directly reviewed in an emulator. |
| DQ-39 / READY | Luna / assets | Audit the Japanese `user_interface` texture row exported from `data/loading/logo.tex` for embedded Japanese wording or other language-bearing marks. Read-only review; do not edit the baseline or create an override. | The exact `graphics/index.json` row for `disc!/_DATA.YFS;1!/data/loading/logo.tex`, its `graphics/user_interface/00039_00889_00000_konami_jp.tga` image, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | `tasks/GRAPHIC_TEXT_LOADING_LOGO.md` plus scoped success/failure evidence; record the source/TGA hashes, dimensions, header/extent, visible text/bounds or negative finding, and visual review. Preserve Japanese baseline and make no runtime/UV inference. |
| DQ-40 / COMPLETE (integrated as `743ec3e`; worker source `d0be870`) | Luna / assets | Audited all 16 Japanese character-summary textures from `lib_char_sum.b`. All are distinct, fully opaque 64×64 portraits/crops with no readable translation-bearing text; source baselines were preserved. | The exact `graphics/index.json` rows whose `asset_path` is `disc!/_DATA.YFS;1!/data/menu/lib_char_sum.b` and category is `characters`, their mapped TGA images, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_LIB_CHAR_SUM.md`](../tasks/GRAPHIC_TEXT_LIB_CHAR_SUM.md) and scoped success evidence; all 16 row selections, filenames, dimensions, full hashes, headers/extents, opacity, and visual findings are recorded. Worker author/trailers are retained on integration. |
| DQ-41 / COMPLETE (integrated as `d1aedad`; worker source `dafa268`) | Luna / assets | Audited all 16 Japanese `lib_voice_sum.b` textures. The distinct, fully opaque 64×64 portraits have faint balloon-like marks; enlarged dash/dot strokes remain unclassified, and no Japanese names, voice labels, or Latin text are readable. | The exact `graphics/index.json` rows whose `asset_path` is `disc!/_DATA.YFS;1!/data/menu/lib_voice_sum.b` and category is `characters`, their mapped TGA images, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). | [`tasks/GRAPHIC_TEXT_LIB_VOICE_SUM.md`](../tasks/GRAPHIC_TEXT_LIB_VOICE_SUM.md) and scoped success evidence; filenames, dimensions, full hashes, headers/extents, alpha and visual review are recorded. Worker author and trailers are retained on integration. |
| DQ-42 / READY | Luna / assets | Audit the 16 Japanese training-character textures for embedded names, labels, or other translation-bearing artwork. Read-only review: do not edit `_jp.tga` files or create overrides. | The exact `graphics/index.json` rows whose `asset_path` starts with `disc!/_DATA.YFS;1!/data/menu/train_char_tex_` and category is `characters`, plus their mapped TGA images and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). This scope excludes the separate `train_tex.b` menu bundle and has no image-hash duplicates among DQ-40 or DQ-41. | `tasks/GRAPHIC_TEXT_TRAIN_CHAR_TEX.md` plus a scoped `docs/SUCCESSES.md` or `docs/FAILURES.md` entry; inventory all 16 filenames, dimensions and hashes, TGA headers/extents, visible text/bounds or negative findings, and visual review. Preserve Japanese baselines and make no UV/runtime inference. |
| DQ-43 / READY | Luna / assets | Audit the four Japanese character textures from `char_00_tex.b` for embedded names, labels, or other translation-bearing artwork. Read-only review: do not edit `_jp.tga` files or create overrides. | The exact four `graphics/index.json` rows whose `asset_path` is `disc!/_DATA.YFS;1!/data/menu/char_00_tex.b` and category is `characters`, their mapped `graphics/characters/00039_00977_000*_jp.tga` images, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). The four 512×256 files have unique hashes and no SHA-256 duplicates among DQ-40 or DQ-41. | `tasks/GRAPHIC_TEXT_CHAR_00.md` plus scoped success/failure evidence inserted after `## Icon textures include reusable Japanese command-label atlases`; record all four filenames, dimensions, hashes, TGA headers/extents, visible text/bounds or negative findings, and visual review. Preserve Japanese baselines; make no UV/runtime inference. |
| DQ-44 / READY | Luna / assets | Audit the four Japanese character textures from `char_01_tex.b` for embedded names, labels, or other translation-bearing artwork. Read-only review: do not edit `_jp.tga` files or create overrides. | The exact four `graphics/index.json` rows whose `asset_path` is `disc!/_DATA.YFS;1!/data/menu/char_01_tex.b` and category is `characters`, their mapped `graphics/characters/00039_00979_000*_jp.tga` images, and [`LOCALIZATION_METHODOLOGY.md`](LOCALIZATION_METHODOLOGY.md). The four 512×256 files have unique hashes and no SHA-256 duplicates among DQ-40, DQ-41, or DQ-43. | `tasks/GRAPHIC_TEXT_CHAR_01.md` plus scoped success/failure evidence inserted after `## Effect-category textures contain no readable localized wording`; record all four filenames, dimensions, hashes, TGA headers/extents, visible text/bounds or negative findings, and visual review. Preserve Japanese baselines; make no UV/runtime inference. |
| DQ-45 / READY | Luna / localization graphics | Audit the remaining `00039_01566_0003_title_parts` English title strip for MÄR HEAVEN brand styling against the owner-supplied reference. Read-only review: do not edit `_jp.tga`, `_eng.tga`, or create overrides. Determine whether `MÄR HEAVEN` and `ÄRM FIGHT DREAM` are separate text roles and record their measured bounds; do not infer runtime or UV use. | The exact `graphics/index.json` row for `disc!/_DATA.YFS;1!/data/menu/title_tex.b` and member `0003_title_parts.txc.bin`, its mapped Japanese and English 512×256 TGAs, DQ-38's logo source crops, and the reference digest `33cbe51d2ac9c5fa409ca023ab9147f67a78304ca9f25328932540b51643e947`. This row is separate from DQ-38's `title_marh` member. | `tasks/GRAPHIC_TEXT_TITLE_PARTS_STYLE.md` plus a scoped success/failure result; verify row/source/TGA hashes, dimensions/header/extent, inspect phrase bounds against the reference, and preserve both baseline and existing English sibling. Make no art or ISO changes. |

The `ttlprts` and repeated `title000` overrides remain integrated with their
prior round-trip evidence. DQ-14's database conditions, DQ-15's `EQUIP ARM`
command, and DQ-16's button-help labels now join the current 14-graphic English
ISO build; all three edited text/catalog resources and all 14 graphics reparse
byte-exactly. Japanese baselines remain unchanged, and emulator/runtime review
is still open. DQ-23's GameOver and DQ-29's transition-menu audits are
integrated. DQ-34 and DQ-35 await lead RTX3/ISO validation. DQ-36's remaining
small-menu audit is integrated as `8ff964d`; DQ-31's TGS-menu audit is
integrated as `2ea24f0`, and DQ-37's full-width menu audit is integrated as
`df4315d`. DQ-38 tracks title-logo rework. DQ-39, DQ-42 through DQ-45 replenish the
ready reserve.
The remaining audits are the bounded cards above.

| Priority | Task | State / next evidence | Completion gate |
| --- | --- | --- | --- |
| P0 | Full boot ELF reconstruction | Bootstrap baseline implemented; 3,528 object bytes plus 3,441,676 raw bytes | Full `cmp`, isolated source-only rebuild, regression tests |
| P1 | Recover trivial linkonce accessor cluster | Census done (`reports/linkonce_text_inventory.json`): 1,694 named sections/71,932 bytes/497 classes; 515 sections/95 classes recovered (441 trivial 8-byte + 74 from three non-trivial-tier batches, 12/16/20-byte). Every trivial section not judged UI/dialog/texture/model/movie-adjacent is now recovered or explicitly deferred (5 `$gp`-relative-static; 7 more for isolated-probe register allocation/scheduling; 3 more for confirmed R5900-specific instructions (`LQ`/`SQ` and a 3-operand `MULT` variant)). 283 sections remain deliberately excluded as UI/dialog/texture/model/movie-adjacent, out of scope while the concurrent localization session works there; roughly 260 non-trivial (>8-byte) sections remain unexamined. `objVector`'s size was corrected from 8 to 16 bytes (aligned) after a real field-copy method disambiguated it; all previously-verified sections re-confirmed unaffected. See [the linkonce cluster task](tasks/LINKONCE_CLUSTER.md) | Same method per section: disassemble, natural one-line accessor, `compare_sections.py`, full `cmp` |
| P1 | Broaden compiler identification | EE GCC 2.96 now matches 515 sections across 95 classes (const methods, pointer-return and pointer-argument stubs, fixed-return and no-op methods, unconditional field-zeroing, a float pass-through, a by-value class parameter, self-referencing pointers, enum-typed parameters, a T-back-reference-compressed repeated parameter with the exact index rule confirmed, a Q-qualified nested enum, `RC`/const-reference on both classes and primitives, a 16-bit `short` load, 64-bit `unsigned long` fields via `ld`/`sd`, a `const char*` parameter, a computed (non-passthrough) arithmetic return, `movn` conditional-move ternaries, signed vs. unsigned zero-comparisons, a setter returning its own argument, embedded in-object arrays, a `nor`-based bitwise-NOT idiom, a union for a dual-typed field, a null-safe linked-list traversal through a parameter, a generalized N-statement scheduler rule), unmodified `-O2` | Representative nontrivial C++ methods/callers (struct-return, matrix copies), defensible common ABI/flags; no per-function steering |
| P1 | Recover CCamera/CCamera2/CCameraMv class evidence | Partial layout candidates for 3 classes exist; [GetViewRect probe](tasks/VIEW_RECT_PROBE.md) differs (40 vs 24 bytes, unaligned vs aligned copies). [Camera matrix probe](tasks/CAMERA_MATRIX_PROBE.md) establishes `objMatrix` as a 64-byte, 8-byte-aligned aggregate via the compiler's own aligned-vs-defensive codegen choice, but getters still mismatch on register allocation and setters use undecoded R5900 `LQ`/`SQ` quadword instructions outside current tooling. [RTTI evidence](tasks/TYPEINFO_SECTIONS.md) (DQ-07, complete) confirms `CCamera` derives from `C3dObject` and `CCamera2` from `CCamera` via each class's own compiler-synthesized type-info function | Justified layout/signatures plus authentic-source review and complete ELF comparison |
| P1 | Reduce main ELF raw regions | 3,441,676 bytes remain raw across many small intervals (was 3,445,140 at initial bootstrap); the safe-scope linkonce cluster is exhausted, so further reduction needs either the deferred `$gp`-relative statics or the excluded UI/dialog/texture/model/movie sections | Full byte equality and provenance for each replacement |
| P1 | Decode and localize UI graphics | All 724 menu `.b` leaves decode to declared sizes. Of these, 721 use a validated 32-byte resource table: 3,084 members were extracted and all 721 untouched bundles rebuild byte-exactly. `graphics/` indexes all 30,397 catalogued TXCs; 239,444,320/239,584,968 TXC bytes have editable TGA exports (99.9413% by TXC bytes; 30,354/30,397 records). The remaining 140,648 bytes are 43 short PSMCT32 records. Current full-payload editability is B/Y=888,328,887/1,303,947,016 (68.1261%), including 615,212,537 editable movie-video bytes and 33,583,536 YOBJ coordinate bytes. All 30,354 editable `_jp.tga` baselines and the graphics index are version-controlled; Japanese baselines remain unchanged. Fourteen same-size `_eng.tga` siblings override title and UI art in `mar_eng.iso`. The latest export/stage audit found fourteen overrides, 43 unresolved records and zero modified Japanese TGAs. `make verify-graphics-image` reparses and byte-compares all fourteen staged English TXCs from the ISO (1,400,064 bytes). Synthetic English reinsertion tests cover nested UI-table/BPE/PAC and standalone TXC/PAC paths. The literal-identity BPE writer grows edited `title_tex.b` from 301,071 to 922,091 bytes. All 2,328 direct and nested AT3 resources now parse 26,798 named-node envelopes and rebuild byte-exactly; their 192-byte and repeated 112-byte regions, UV meanings, additional title variants and runtime display remain unresolved. See [graphic localization evidence](../tasks/GRAPHIC_TEXT_LOCALIZATION.md), [title rendering evidence](../tasks/TITLE_TEXTURE_RENDERING.md), [the readable census](../reports/asset_recovery_census.md), and [census data](../reports/asset_recovery_census.json). | Validate current title/`windisp` UV and runtime display; continue the queued corpus audit for Japanese text; investigate the 43 short PSMCT32 records and alternate `.yma` structures |
| P2 | Inventory DVP overlays and IOP modules | Inventory evidence is in `reports/dvp_overlay_inventory.json`, `tasks/DVP_OVERLAY_INVENTORY.md`, and `tasks/IOP_MODULE_INVENTORY.md`: 40 DVP overlay sections (32,232 body bytes) map one-to-one to 40 12-byte table records; 14 standalone IRX reports and 16 IOPRP module records are catalogued, with an exact no-op IOPRP reconstruction. Loader semantics, custom fixups, and module behavior remain unresolved. | Correlate overlay instructions with loader/fixup behavior and module exports/callers; preserve unknown fields; validate execution/runtime before semantic claims |
| P2 | Inventory AFS/YFS/IOPRP internals | AFS TOC inventory is implemented and tested for both archives in `tasks/AFS_INTERNALS.md`; all 38 member starts are 2048-byte aligned and a synthetic no-op rebuild is exact. `tasks/AFS_TOC_SUFFIX.md` records the suffix's date/time-like first 12 bytes and exact member-table correlation in its final word, while semantic names and intent remain unresolved. Detailed YFS and IOPRP semantics remain open. | Establish remaining YFS/IOPRP structures and AFS suffix meaning; test malformed input, lossless rebuild, edited relocation, and runtime behavior |
| P1 | Asset workspace and translation surfaces | Full-disc unchanged rebuild matches; tracked catalogues cover `_msg.dat`, `CardList.txt`, and `DataBase.txt`. UI `.b` wrappers decode; 721 resource tables and 3,084 members have exact no-op reassembly and edited growth/repoint support; 3 alternate `.yma` layouts remain raw. The root `graphics/` index covers all 30,397 catalogued TXCs. The census separates Z/Y=86.8157%, A/Y=100%, B/Y=68.1261%, and C/Y=0%; Y=1,303,947,016 and Y-B=415,618,129 bytes, with byte-weighted categories in the report. All 13 SofDec movies export as immutable Japanese MPEG-2/ADX streams; profile-compatible video overrides remux with preserved ADX and CRI tags, and decoded no-op comparisons pass. B counts only 615,212,537 video elementary-stream bytes; movie audio/container overhead remain in Y-B. A disposable full mod-ISO build with a test clip re-parsed the changed movie and preserved its original ADX; emulator playback is pending. Japanese `*_jp.tga` baselines pair with English `_eng.tga` siblings. Fourteen English TXCs (1,400,064 bytes) reparse exactly from the relocated ISO; nested and standalone override tests cover reinsertion. The YOBJ editor covers XYZ coordinates only. `make build-mod-disc` writes `mar_eng.iso` in the workspace root. | Review CardList names/titles and current title/UI/weapon graphic drafts; validate localized layout and movie playback in an emulator; investigate AT node properties, remaining YOBJ fields, alternate `.yma` layouts, append placement; see the [CardList review](../tasks/CARDLIST_TRANSLATION_REVIEW.md), [asset method](TASK_ASSET_WORKSPACE_METHODOLOGY.md), [readable census](../reports/asset_recovery_census.md), and [census data](../reports/asset_recovery_census.json) |
| P1 | Decode YOBJ/YMP model geometry | `tools/yobj.py` validates 899 direct YMPs (197,085,308 bytes) plus 14 nested UI-bundle YMPs; all 913 parse and rebuild byte-exactly, with 807,439 POF0 pointer slots audited. `tools/yobj_geometry.py` exports and same-size rebuilds 9,976 meshes / 1,399,314 vertices, exposing 33,583,536 editable XYZ position/normal source bytes. A synthetic nested edit survives UI-table/BPE/PAC reinsertion. Total B/Y is 68.1261%; other model fields and runtime semantics remain unvalidated, and internal growth is unsupported. YPC files remain unresolved. | Correlate coordinate meaning with the game draw path/runtime; recover topology/UVs and other model fields; establish POF0 fixups before internal growth; test changed YMP through the full PAC/YFS/ISO path and runtime; see [YOBJ evidence](../tasks/YOBJ_MODEL_RECOVERY.md) |
| P2 | Full-disc reconstruction and relocation | Untouched image matches exactly. Current translated image is 5,023,940,608 bytes (SHA-256 `9590a689a512d035e8073f44917eb78b6ee35841e549c61dd065641230e25e67`); all fourteen staged English TXCs (1,400,064 bytes) reparse byte-exactly, and the pinned-reference comparison authenticates the baserom and reports 758,496,753 differing bytes in `reports/mar_eng_compare_14_graphics_database.json`. Append placement carries the rebuilt YFS; `make build-mod-disc` atomically replaces an existing ISO only after success. See [graphic localization evidence](../tasks/GRAPHIC_TEXT_LOCALIZATION.md). | Runtime acceptance, broader relocation cases and a more space-efficient placement strategy |
| P3 | Independent provenance / runtime checks | Not performed | External dump provenance and recorded runtime validation |

Completed enabling work: ISO/ELF inventory, lossless asset export/rebuild and
streaming whole-disc comparison, selected assembly preservation,
historical compiler probe, explicit boot reconstruction, isolated rebuild check,
mandatory commit attribution trailers, and a linkonce-cluster census tool.

Any source promotion must satisfy authenticity independently of byte equality.
Do not treat P0 bootstrap completion as recovered-code or whole-game completion.
